Start server:

`cd server`
`npm start`

Start web app:

`cd book-app`
`npm start`

Coverage tests:

`npm test -- --coverage`

After run build:

  `npm install -g serve`
  `serve -s build`


TODO:
- Add messsage for action notifications
- Run tests
- Publish to github pages
